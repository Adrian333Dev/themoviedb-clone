import { FC } from 'react';
import { MoreMenuList } from '../styles/movie-card';

const MoreMenu: FC = () => {
	return (
		<MoreMenuList>
			Typog
		</MoreMenuList>
	);
};

export default MoreMenu;
